"""
Web presentation layer.

This module is a thin delivery adapter that serves the browser UI.
It intentionally contains no domain logic: it only exposes the static
single-page app that talks to the existing domain HTTP endpoints.
"""
